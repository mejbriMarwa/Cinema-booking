// Config starter code
import { createChatBotMessage } from "react-chatbot-kit";

const Config = {
  initialMessages: [createChatBotMessage(`Hello`)],
  botName:"Movie Ticket Booking System"
};

export default Config;
